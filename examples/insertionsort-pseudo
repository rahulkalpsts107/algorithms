#insertion-sort
a = [10,8,9,7,1]
for j in range(1,len(a)):
    key = a[j]
    i = j-1
    while i>=0 and a[i] > key :
        print(i,j)
        a[i+1] = a[i]
        i = i-1
    a[i+1] = key
    j+=1
print(a)